# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.venv/
venv/
.env
.env.*

# OS
.DS_Store
Thumbs.db

# Data (keep schema in repo)
data/*.csv
!data/glove_data_schema.csv

# Large data folders (professor's dataset - 178MB)
data/Data/

# Windowed feature files (generated, large)
data/*.npz

# Trained models (large binary files - RF model is 156MB)
# Keep the models directory structure but ignore the actual model files
models/*.pkl
models/*.h5
models/*.tflite
models/*.onnx
models/windowed/*.pkl
models/windowed/*.h5

# Jupyter (uncomment if you add notebooks)
# .ipynb_checkpoints/
